const fs = require('fs');
const path = require('path');

function getDirectoryContents(files, currentDir, query) {
}

function isDirectory(currentDir, file) {
    const fileInfo = path.join(currentDir, file);
}

function readDir(currentDir, res, query) {
}

exports.get = (req, res) => {
};
